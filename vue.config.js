/*
module.exports = {
  publicPath: process.env.NODE_ENV === 'production'
    ? '/portfolio/'
    : '/'
 }
 */

 module.exports = {
  publicPath: process.env.NODE_ENV === 'production'
    ? '/a-matias.github.io/portfolio/'  // Cambia a tu ruta real en GitHub Pages
    : '/'
}

